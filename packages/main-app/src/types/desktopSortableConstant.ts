import { defaultNamespace } from '@/hooks/useNamespace'

export const APP_CLASS_NAME = `${defaultNamespace}-desktop-controller__apps`
export const FOLDER_CLASS_NAME = `${defaultNamespace}-desktop-folder-modal-body__desktop__apps`
